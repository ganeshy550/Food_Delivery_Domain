server:
  port: 7019
spring:
  datasource:
    url: jdbc:mysql://restaurant-db:3306/restaurant_info
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
